import { lazy, Suspense } from "react";
import { LoadingState } from "./loading-state";

// Onboarding is a page component and doesn't need lazy loading
// This file can be used for other large components if needed
